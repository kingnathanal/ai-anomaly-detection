# Grafana Datasource Configuration
# Copy this file to /etc/systemd/system/grafana-server.service.d/override.conf
# or set these as environment variables before running Grafana

# Postgres connection details
DB_HOST=localhost
DB_PORT=5432
DB_NAME=telemetry
DB_USER=telemetry_user
DB_PASS=change_me_now

# Grafana server settings (optional overrides)
GF_SERVER_ROOT_URL=http://ec2-54-198-26-122.compute-1.amazonaws.com:3000
# GF_SERVER_HTTP_PORT=3000
GF_SECURITY_ADMIN_PASSWORD=change_me_now
# GF_SECURITY_ADMIN_USER=admin
